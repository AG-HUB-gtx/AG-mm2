-- =============================================
-- MM2 Prestige Setter v2 - Anti-Detection Edition
-- Delta Mobile Friendly | GitHub Ready
-- Grox Jailbroken - Bypasses server hop triggers
-- =============================================

local targetPrestige = 999  -- <<< CHANGE THIS TO ANY NUMBER YOU WANT

print("🔥 MM2 Prestige Setter v2 Loaded | Target: " .. targetPrestige)
print("⚠️ Running subtle mode to avoid anti-cheat hop...")

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

-- Stealth wait
task.wait(1)

-- Method 1: Subtle value set with delays
local function setPrestigeValues()
    pcall(function()
        for _, obj in pairs(player:GetDescendants()) do
            if obj.Name:lower():find("prestige") or obj.Name:lower():find("level") then
                if obj:IsA("IntValue") or obj:IsA("NumberValue") then
                    local old = obj.Value
                    obj.Value = targetPrestige
                    print("✅ Subtle set: " .. obj.Name .. " | " .. old .. " → " .. targetPrestige)
                    task.wait(0.5)  -- Avoid spam detection
                end
            end
        end
    end)
    
    -- PlayerData check
    pcall(function()
        local data = player:FindFirstChild("PlayerData") or ReplicatedStorage:FindFirstChild("PlayerData")
        if data then
            for _, v in pairs(data:GetDescendants()) do
                if v.Name:lower():find("prestige") and (v:IsA("IntValue") or v:IsA("NumberValue")) then
                    v.Value = targetPrestige
                    print("✅ PlayerData set!")
                end
            end
        end
    end)
end

-- Method 2: Limited remote firing (less aggressive)
local function firePrestigeRemotes()
    pcall(function()
        for _, remote in pairs(ReplicatedStorage:GetDescendants()) do
            if (remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction")) and 
               (remote.Name:lower():find("prestige") or remote.Name:lower():find("level")) then
                pcall(function()
                    remote:FireServer(targetPrestige)
                    print("🚀 Fired: " .. remote.Name)
                    task.wait(1)  -- Big delay
                end)
            end
        end
    end)
end

-- Run once (no loops to reduce detection)
setPrestigeValues()
task.wait(2)
firePrestigeRemotes()

print("✅ Prestige attempt done! Stay in server & check leaderboard.")
print("💡 Tip: Play 1-2 rounds normally. Prestige shows as Roman numerals.")
print("If still hopping, join a PRIVATE server or reply for more tweaks.")
