-- Grok OYB Panel Opener / Force Loader
-- Specifically hunts for and opens the "OYB Panel"

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local StarterGui = game:GetService("StarterGui")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

print("🔍 Grok OYB Panel Cracker Loaded - Targeting 'OYB Panel'")

local function forceOpenPanel(panel)
    if not panel then return end
    panel.Enabled = true
    panel.Visible = true
    panel.ResetOnSpawn = false
    
    -- Make all children visible
    for _, child in ipairs(panel:GetDescendants()) do
        if child:IsA("Frame") or child:IsA("ScrollingFrame") or child:IsA("ImageLabel") then
            child.Visible = true
            child.BackgroundTransparency = 0
        elseif child:IsA("TextButton") or child:IsA("TextLabel") then
            child.Visible = true
        end
    end
    
    print("✅ OYB Panel FORCED OPEN:", panel:GetFullName())
    StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.All, true) -- Extra visibility push
end

-- Test 1: Direct name search in PlayerGui
local oybPanel = LocalPlayer.PlayerGui:FindFirstChild("OYB Panel", true) 
    or LocalPlayer.PlayerGui:FindFirstChild("OYBPanel", true)
    or LocalPlayer.PlayerGui:FindFirstChild("OYB_Panel", true)

if oybPanel then
    forceOpenPanel(oybPanel)
else
    print("❌ OYB Panel not found in PlayerGui yet - scanning deeper...")
end

-- Test 2: Deep scan everywhere
spawn(function()
    for _, gui in ipairs(LocalPlayer.PlayerGui:GetDescendants()) do
        if gui:IsA("ScreenGui") and (gui.Name:find("OYB") or gui.Name:lower():find("oyb")) then
            forceOpenPanel(gui)
        end
    end
    
    -- Check CoreGui and ReplicatedStorage too
    pcall(function()
        for _, gui in ipairs(CoreGui:GetDescendants()) do
            if gui:IsA("ScreenGui") and (gui.Name:find("OYB") or gui.Name:lower():find("oyb")) then
                forceOpenPanel(gui)
            end
        end
    end)
end)

-- Test 3: Remote firing for OYB commands
for _, obj in ipairs(ReplicatedStorage:GetDescendants()) do
    if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
        if obj.Name:find("OYB") or obj.Name:lower():find("oyb") or obj.Name:lower():find("panel") then
            print("📡 Found OYB-related remote:", obj:GetFullName())
            pcall(function()
                if obj:IsA("RemoteEvent") then
                    obj:FireServer("open")
                    obj:FireServer("showpanel")
                    obj:FireServer("admin")
                    obj:FireServer("OY

