-- Grok Kohl's Admin Owner Spoof
-- Targets Owner rank (5) and Game Creator (6)

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

print("🔥 Kohl's Admin Owner Spoof Loaded")

local function huntAndInject()
    print("🔍 Hunting for Kohl's Admin settings...")
    for _, obj in ipairs(game:GetDescendants()) do
        local n = obj.Name:lower()
        if n:find("kohl") or n:find("admin") or (obj.Name == "Settings" and obj:IsA("ModuleScript")) then
            print("🎯 Kohl/Settings FOUND:", obj:GetFullName())
            
            pcall(function()
                if obj:IsA("ModuleScript") then
                    local settings = require(obj)
                    if typeof(settings) == "table" then
                        -- Inject into Owners / SuperAdmins
                        if settings.Owners then
                            table.insert(settings.Owners, LocalPlayer.UserId)
                            table.insert(settings.Owners, LocalPlayer.Name)
                            print("✅ Injected into Owners list")
                        end
                        if settings.SuperAdmins then
                            table.insert(settings.SuperAdmins, LocalPlayer.UserId)
                            print("✅ Injected into SuperAdmins")
                        end
                    end
                end
            end)
        end
    end
end

-- Metatable rank spoof
local mt = getrawmetatable(game)
local oldIndex = mt.__index
setreadonly(mt, false)
mt.__index = newcclosure(function(self, key)
    if key == "Rank" or key == "AdminLevel" or key == "IsOwner" or key == "IsAdmin" then
        print("Spoofed rank check -> Owner")
        return 5  -- Owner rank
    end
    return oldIndex(self, key)
end)
setreadonly(mt, true)

local function spamOwnerCommands()
    print("📡 Spamming Owner commands...")
    for _, remote in ipairs(ReplicatedStorage:GetDescendants()) do
        if remote:IsA("RemoteEvent") then
            pcall(function()
                remote:FireServer(":oa " .. LocalPlayer.Name)
                remote:FireServer(":rank " .. LocalPlayer.Name .. " 5")
                remote:FireServer(":admin " .. LocalPlayer.Name)
                remote:FireServer(":settings")
            end)
        end
    end
end

-- Owner button
local sg = Instance.new("ScreenGui")
sg.ResetOnSpawn = false
sg.Parent = LocalPlayer.PlayerGui

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0, 280, 0, 90)
btn.Position = UDim2.new(0, 30, 0.35, 0)
btn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
btn.Text = "🔥 KOHL OWNER SPOOF\nClick to Spam"
btn.TextScaled = true
