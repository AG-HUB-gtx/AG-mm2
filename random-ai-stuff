-- Grok Super Admin Grant v12 - Coin Simulator X (AG Jailbreak)
-- Attempts to force super admin + OYB Panel access

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

print("🔥 AG JAILBREAK SUPER ADMIN GRANT v12 Loaded")

-- Metatable hook for logging + potential spoof
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    if method == "FireServer" or method == "InvokeServer" then
        print("[REMOTE LOG] " .. self:GetFullName() .. " | Args: ", unpack(args))
    end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

local function spamSuperAdmin()
    print("📡 SPAMMING SUPER ADMIN GRANT ATTEMPTS...")
    for _, remote in ipairs(ReplicatedStorage:GetDescendants()) do
        if remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction") then
            task.spawn(function()
                pcall(function()
                    -- Common admin grant patterns
                    remote:FireServer("giveadmin", LocalPlayer, "superadmin", true)
                    remote:FireServer("setrank", LocalPlayer, "superadmin")
                    remote:FireServer("grant", "owner", "OYB")
                    remote:FireServer("adminabuse", "enable", LocalPlayer.UserId)
                    remote:FireServer("OYB Panel", "activate")
                    remote:FireServer("abuseeventpanel", "grant")
                    remote:FireServer("addadmin", LocalPlayer.Name, 999)
                end)
            end)
        end
    end
end

local function injectAdminValues()
    print("💉 Injecting admin flags...")
    for _, v in ipairs(LocalPlayer:GetDescendants()) do
        if v:IsA("BoolValue") or v:IsA("StringValue") or v:IsA("NumberValue") then
            if v.Name:lower():find("admin") or v.Name:lower():find("rank") or v.Name:lower():find("permission") then
                v.Value = true
                print("Injected admin flag:", v:GetFullName())
            end
        end
    end
end

-- Persistent fake + real attempt button
local sg = Instance.new("ScreenGui")
sg.Name = "SuperAdminInjector"
sg.ResetOnSpawn = false
sg.Parent = LocalPlayer.PlayerGui

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(0, 250, 0, 80)
btn.Position = UDim2.new(0, 30, 0.3, 0)
btn.BackgroundColor3 = Color3.fromRGB(255, 0, 100)
btn.Text = "🔴 GRANT SUPER ADMIN\n(Spam for OYB Panel)"
btn.TextScaled = true
btn.TextColor3 = Color3.new(1,1,1)
btn.Parent = sg

btn.MouseButton1Click:Connect(function()
    print("🚀 SUPER ADMIN GRANT TRIGGERED")
    spamSuperAdmin()
    injectAdminValues()
end)

-- Auto spam loop
spawn(function()
    while wait(1.2) do
        spamSuperAdmin()
        injectAdminValues()
    end
end)

print("✅ v12 loaded. Click the red button repeatedly.")
print("Watch console for remote logs. If super admin granted, OYB Panel should appear.")
print("If kicked → strong server-side anti-cheat on admin system.")

game:GetService("UserInputService").InputBegan:Connect(function(i, gp)
    if gp then return end
    if i.KeyCode == Enum.KeyCode.Insert then
        spamSuperAdmin()
    end
end)
